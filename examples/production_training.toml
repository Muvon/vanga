# Production Training Config (RECOMMENDED)
[training_params]
epochs = { Auto = { max_epochs = 1000 } }
learning_rate = { Adaptive = { initial_lr = 0.01 } }
validation_split = 0.2
early_stopping_patience = 50
gradient_clip = 1.0
